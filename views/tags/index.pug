extends ../layout


block header
    p Tag index
    p.abstract
        | #[strong Abstract.] The list below shows all the tags used in posts. Sorted by usage.

block content
    div.uk-section

        div.uk-section.uk-section-small.uk-padding-small
            div.uk-container.uk-text-center
                span
                    each tag in tags
                        span.comma.uk-text-large #[a(href="/tags/"+tag._id) #{tag._id} (#{tag.count})]
                        | 
block footer
    ol
        li Permalink: 
            a(href="/tags/") /tags
        li Response time: #{ms}ms